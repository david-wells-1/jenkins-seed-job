pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'Test Jenkinsfile - web-1/test job is working on the test-branch-parameter branch'
            }
        }
    }
}
