pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'Test Jenkinsfile - app-1 job is working on the test-branch-parameter branch'
            }
        }
    }
}
